.admin-permissions-user(data-user=user.id)
  img(src=user.avatar)
  p(title=user.displayName) #{user.displayName} - #{user.email}
  - if (canRevoke)
    button.btn.btn-danger(type='button', data-revoke=user.id, data-user=user.fullName)= t('admin-permissions.revoke')